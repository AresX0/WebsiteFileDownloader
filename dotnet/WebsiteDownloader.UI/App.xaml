<Application x:Class="WebsiteDownloader.UI.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:WebsiteDownloader.UI.Converters"
             Startup="OnAppStartup">
    <Application.Resources>
        <ResourceDictionary>
            <!-- Converters -->
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
            <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
            <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter" />
            <converters:FileSizeConverter x:Key="FileSizeConverter" />
            <converters:InverseBoolConverter x:Key="InverseBoolConverter" />

            <!-- Color palette (dark theme — default) -->
            <SolidColorBrush x:Key="PrimaryBrush" Color="#FF1976D2"/>
            <SolidColorBrush x:Key="PrimaryDarkBrush" Color="#FF1565C0"/>
            <SolidColorBrush x:Key="AccentBrush" Color="#FF00C853"/>
            <SolidColorBrush x:Key="DangerBrush" Color="#FFD32F2F"/>
            <SolidColorBrush x:Key="WarningBrush" Color="#FFF57F17"/>
            <SolidColorBrush x:Key="SurfaceBrush" Color="#FF1E1E2E"/>
            <SolidColorBrush x:Key="SurfaceLightBrush" Color="#FF2A2A3C"/>
            <SolidColorBrush x:Key="TextPrimaryBrush" Color="#FFE0E0E0"/>
            <SolidColorBrush x:Key="TextSecondaryBrush" Color="#FFA0A0B0"/>
            <SolidColorBrush x:Key="BorderBrush" Color="#FF3A3A4C"/>

            <!-- Base button style -->
            <Style x:Key="ActionButton" TargetType="Button">
                <Setter Property="Padding" Value="16,8"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    CornerRadius="4"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
                <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
                <Setter Property="Foreground" Value="White"/>
            </Style>

            <Style x:Key="SuccessButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
                <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                <Setter Property="Foreground" Value="White"/>
            </Style>

            <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
                <Setter Property="Background" Value="{StaticResource DangerBrush}"/>
                <Setter Property="Foreground" Value="White"/>
            </Style>

            <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
                <Setter Property="Background" Value="{DynamicResource SurfaceLightBrush}"/>
                <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
